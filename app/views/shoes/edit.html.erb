<h1>Editing Book</h1>
<%= form_for(@shoe) do |f| %>
<% if @shoe.errors.any? %>
<table>
	<font color="red">
       <p>2 errors prohibited this book from being saved:</p>
         <ul>
            <li>Title can't be blank</li>
            <li>opinion can't be blank</li>
         </ul>
    </font>
</table>
<% end %>

<h4>Title</h4>
<%= f.text_field :title %>
<h4>opinion</h4>
<%= f.text_area :body %>
<%= f.submit 'Update Book'%>
<% end %>
<%= link_to "Show", shoe_path(@shoe.id) %>
<%= link_to "Back", shoes_path %>
